 <center><h3> New User Sign Up: </h3></center>

    <%= form_with url:'/users', local: :true do |f| %>
        <%= f.label :email %>
        <%= f.text_field :email  %>
        <br>  
        <%= f.label :password %>
        <%= f.password_field :password %>
        <br>
        <%= f.label "First Name" %>
        <%= f.text_field :first_name  %>
        <br>
        <%= f.label "Last_name" %>
        <%= f.text_field :last_name %>
        <br>
        <%= f.label :age %>
        <%= f.text_field :age %>
        <br>
        <%= f.label :about %>
        <%= f.text_area :about %>
        <br>
        <%= f.label "Check the box if you wish to sign up as a Landlord" %>
        <%= f.check_box "landlord" %>
     


      <span class="pull-left"><%= f.submit "Create New User"  %></span>
   <% end %>

   <% if !@user.nil? && @user.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@user.errors.count, "error") %>
      prohibited this user from being created:
    </h2>
 
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% if !@tenant.nil? && @tenant.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@tenant.errors.count, "error") %>
      prohibited this user from being created:
    </h2>
 
    <ul>
    <% @tenant.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% if !@landlord.nil? && @landlord.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@landlord.errors.count, "error") %>
      prohibited this user from being created:
    </h2>
 
    <ul>
    <% @landlord.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>